import type { JourneyYear } from '../../types/content'
interface JourneyYearPanelProps { readonly year: JourneyYear; readonly active: boolean }
export function JourneyYearPanel({ year, active }: JourneyYearPanelProps) { return <article id={`journey-panel-${year.id}`} className={`journey-year-panel${active ? ' is-active' : ''}`} role="tabpanel" aria-labelledby={`journey-tab-${year.id}`}><div className="journey-panel-heading"><span>{year.label}</span><h3>{year.title}</h3><p>{year.thesis}</p></div><div className="journey-panel-list"><span>PRIORITIES</span><ul>{year.priorities.map((item) => <li key={item}>{item}</li>)}</ul></div><div className="journey-panel-list"><span>BUILD</span><ul>{year.buildItems.map((item) => <li key={item}>{item}</li>)}</ul></div><div className="journey-panel-outcome"><span>OUTCOME</span><p>{year.outcome}</p></div></article> }
