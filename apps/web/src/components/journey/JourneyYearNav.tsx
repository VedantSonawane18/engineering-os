import type { JourneyYear } from '../../types/content'
interface JourneyYearNavProps { readonly years: readonly JourneyYear[]; readonly activeIndex: number; readonly onSelect: (index: number) => void }
export function JourneyYearNav({ years, activeIndex, onSelect }: JourneyYearNavProps) { return <div className="journey-year-nav" role="tablist" aria-label="Four-year engineering journey">{years.map((year, index) => <button key={year.id} type="button" role="tab" aria-selected={index === activeIndex} aria-controls={`journey-panel-${year.id}`} id={`journey-tab-${year.id}`} className={index === activeIndex ? 'is-active' : ''} onPointerEnter={(event) => { if (event.pointerType === 'mouse') onSelect(index) }} onFocus={() => onSelect(index)} onClick={() => onSelect(index)}><span>{year.number}</span><strong>{year.title}</strong></button>)}</div> }
