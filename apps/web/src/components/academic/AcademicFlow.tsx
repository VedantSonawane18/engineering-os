import type { AcademicFactor } from '../../types/content'
interface AcademicFlowProps { readonly factors: readonly AcademicFactor[]; readonly activeIndex: number; readonly onSelect: (index: number) => void }
export function AcademicFlow({ factors, activeIndex, onSelect }: AcademicFlowProps) { return <div className="academic-flow" role="tablist" aria-label="Academic system factors"><div className="academic-flow__line"><span style={{ transform: `scaleX(${(activeIndex + 1) / factors.length})` }} /></div>{factors.map((factor, index) => <button key={factor.id} type="button" role="tab" aria-selected={index === activeIndex} aria-controls={`academic-factor-${factor.id}`} id={`academic-tab-${factor.id}`} className={index === activeIndex ? 'is-active' : ''} onPointerEnter={(event) => { if (event.pointerType === 'mouse') onSelect(index) }} onFocus={() => onSelect(index)} onClick={() => onSelect(index)}><span>{String(index + 1).padStart(2, '0')}</span><strong>{factor.label}</strong></button>)}</div> }
